"use strict";  // treat all js code as newer version

// alert(3+3); -  We are using nodeJs , not Brower

console.log(3+3) // code readablity should be high

let name = "Raja"
let age = 29
let isLogIn = false;

console.log([name , age , isLogIn]);

// in JavaSript define 7 Primitive dataType

// number   => 2 to power 53
// bigint   => generally use in Stock Market websites
// strig    => " "
// boolean  => true/false
// null     => standalone value
// undefine => value is not assign
// symbol   => unique

// Object

console.log(typeof(name));
console.log(typeof(age));
console.log(typeof(isLogIn));
console.log(typeof(null));           // null is object ( typeof) 
console.log(typeof(undefine));       // undefined







 
